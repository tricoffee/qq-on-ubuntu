2. 无法判断有几个参数，格式化的源文件之后的参数如果是另一个已有的文件，则会覆盖已有文件的原内容，无法解决 不同参数的情况。

   解决办法
   使用 shell 脚本的 case 分支 判断 $# 参数，分为如下情况 ： 
	1）一个参数
		执行默认操作
	2）两个参数
		判断第一个参数是否文本文件、同时第二个参数不应该是一个已存在的文件
	3）三个参数
		在第二个判断的基础上，判断第三个参数是否为 0-N 个空格符
